name: Shared Package Check

on:
    pull_request:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    shared-package:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: Setup Node
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install Dependencies
              run: npm ci

            - name: Validate Shared Package Boundary
              run: npm run check:shared-package
